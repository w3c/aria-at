/**
 * - keep list of hosts
 * - respond with actionable filters
 * - respond with agent matching filters
 * - update list with agent status
 * - proxy to agents
 */

/**
 * @typedef Agent
 * @property publicId
 * @property privateId
 * @property ip
 * @property lastMessageTime
 * @property {object} info
 */

/**
 * @typedef Client
 * @property publicId
 * @property privateId
 * @property ip
 */
