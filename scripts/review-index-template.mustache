<!DOCTYPE html>
<meta charset="utf-8">
<html>
  <head>
    <title>Test Patterns for Review</title>
    <style>
      table {
        border-collapse: collapse;
        margin-bottom: 1em;
      }

      table, td, th {
        border: 1px solid black;
      }

      th {
        padding: 1em;
      }

      td {
        padding: .5em;
      }

      tr:not(:first-of-type) th {
        padding: .5em;
        text-align: left;
      }

    </style>
  </head>
  <body>

    <h1>Test Patterns for Review</h1>

    <table>
      <tr>
        <th>Pattern</th>
        <th>Index Page</th>
        <th>Review Page</th>
        <th># of Tests</th>
        <th>Commit of Last Change</th>
      </tr>
      {{#patterns}}
          <tr>
            <th>{{name}}</th>
            <td><a href="./tests/{{name}}/index.html">Index</a></td>
            <td><a href="./review/{{name}}.html">Review</a></td>
            <td>{{numberOfTests}}</td>
            <td><a href="https://github.com/w3c/aria-at/commit/{{commit}}" target="_blank">{{commitDescription}}</a></td>
          </tr>
      {{/patterns}}
    </table>

  </body>
</html>
