// sets focus on the tab panel
// Find the first tab panel by its ID, #tabpanel-1, and set focus on it.
const firstPanel = document.querySelector('#tabpanel-1');
if (firstPanel) {
  firstPanel.focus();
}
